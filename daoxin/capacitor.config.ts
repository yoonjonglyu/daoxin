import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yoonjongryu.daoxin',
  appName: 'ABILITY: DaoXin(道心)',
  webDir: 'dist'
};

export default config;
