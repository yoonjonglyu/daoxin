import type { FC } from "react";

const SchedulePage: FC = () => {
  return (
    <div>
      <h1>Schedule Page</h1>
      <div>today / tomorrow / week</div>
      <div>schedule list</div>
      <div>schedule details</div>
    </div>
  );
};

export default SchedulePage;